#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#define print(x, y) if (x != 0) printf ("hfhf") ; else if (x <= 0) printf ("asffsasf") ; int i = 0 ; while (i++ < 100) printf ("aaaa\n");


int main()
{
	int array[4] = {1, 4, 5, 8};

	print(0, 0);

}